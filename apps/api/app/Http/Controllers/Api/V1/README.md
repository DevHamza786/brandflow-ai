# API V1 Controllers

Thin controllers only: validate, authorize, delegate to Actions/Services, return API resources.

Routes will be registered under `/api/v1/*` when the API route file is added.

See [docs/AGENTS.md](../../../../docs/AGENTS.md) §10 API Design Standards.
